---
ip: "192.168.7.7"
memory: 1024
cpus: 2

authorize:
    - ~/.ssh/id_rsa.pub
    - ~/.ssh/id_dsa.pub

keys:
    - ~/.ssh/id_rsa
    - ~/.ssh/id_dsa

networking:
    - public: true

folders:
    - map: /Users/miguelr/Projects/Personal/PHP/webzenter
      to: /var/www/webzenter
      owner: www-data
      group: vagrant
      mount_options: ["dmode=775,fmode=664"]

sites:
    - hostname: webzenter.dev
      to: /var/www/webzenter/web

databases:
    - webzenter
